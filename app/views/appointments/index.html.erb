
<%= @appointments.each do |appointment|%>

  <% link_to appointment.appointment_datetime, appointment_path(appointment.id) %>
  <%= appointment.patients.count%>
  <%= appointment.doctors.count%>

<% end %>
