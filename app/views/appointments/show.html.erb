<ul>

<%= @appointments.each do |appt| %>
    <li> Doctor: <%= link_to "#{appt.doctor.name}", doctor_path(appt.doctor) %> </li>
    <li> Patient: <%= link_to "#{appt.patient.name}", patient_path(appt.patient) %> </li>
    <li> Appointment: <%= appt.appointment_datetime %> </li>
<% end %>

</ul>
