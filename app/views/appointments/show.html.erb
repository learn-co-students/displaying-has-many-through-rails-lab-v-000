<h1>Appointment for <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></h1>

<h3><%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></h3>

<p><%= @appointment.appointment_datetime.strftime("%B %e, %Y at %k:%M") %></p>
