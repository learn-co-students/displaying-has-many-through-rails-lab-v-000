<h1>Appointment</h1>

<h2><%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></h2>
<h3><%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></h3>
<p><%= @appointment.appointment_datetime.strftime("%B %d, at %I:%M") %></p>
