<h1>The Appointment</h1>

<p>
  <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %>
  has an appointment scheduled with Dr. <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %>
  on <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>.
</p>
