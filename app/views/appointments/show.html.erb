  <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %> has an appointment at 
  <%= @appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %> with
  <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %><br>
