<h1>Patients at Seattle Grace Hospital</h1>
<ul>
<% @patients.each do |patient| %>
    <li>Name: <%= link_to patient.name, patient_path(patient) %></li>
    <li>Number of Appointments: <%= patient.appointment_count %> <%= "appointment".pluralize(patient.appointment_count) %></li>
    <br></br>
<% end %>
</ul>
