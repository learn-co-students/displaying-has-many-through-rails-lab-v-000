<h1>All Patients</h1>

<ul>
  <% @patients.each do |patient| %>
    <li><h2>Name: <%= link_to patient.name, patient_path(patient) %><br>
    Number of Appointments: <%= patient.appointments.count %></h2></li>
  <% end %>
</ul>