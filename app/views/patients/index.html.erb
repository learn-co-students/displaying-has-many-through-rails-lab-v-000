<h1>Patients</h1>

<div>
  <ul>
    <% @patients.each do |patient| %>
    <li>Name: <%= link_to patient.name, patient_path(patient) %>
      Number of Appointments: <span><%= patient.appointments.size %></span>
      </li>
       
    <% end %>
  </ul>
</div>
