<h1>Patients Index</h1>

<ul>
  <% @patients.each do |patient| %>
    <li>
      <h3>Name: <%= link_to patient.name, patient_path(patient) %></h3>
      <p>Number of Appointments: <%= patient.appointments.size %></p>
    </li>
  <% end %>
</ul>
