<h1>All Patients</h1>
<ul>
  <% @patients.each do |patient| %>
  <li>Name: <%= link_to patient.name, patients_path(patient) %> Number of Appointments: <%= patient.appointments.count %></li>
  <% end %>
</ul>
