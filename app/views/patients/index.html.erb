<h2>Patient Directory</h2>

<ul>
  <% @patients.each do |patient| %>
    <li>
      Patient Name: <%= link_to patient.name, patient_path(patient) %>
      Number of Appointments: <%= patient.doctors.count %>
      <br><br>
    </li>
  <% end %>
</ul>
