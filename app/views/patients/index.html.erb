<h1> Patients </h1>

<div>
  <ul>
    <% @patients.each do |p| %>
      <li><%= link_to p.name, patient_path(p) %></li>
      <li><%= p.appointment_count %></li>
    <% end %>
  </ul>


<% end %>

</div>
