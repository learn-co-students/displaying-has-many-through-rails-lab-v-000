 <!-- page that displays a link to each patient's show page and the total number of appointments they have. -->

 <h1>Patient's List</h1>

 <ul>
   <% @patients.each do |patient| %>
     <li>Name: <%= link_to patient.name, patient_path(patient.id) %>
        Number of Appointments: <%= patient.appointments.size%></li>
   <% end %>
 </ul>
