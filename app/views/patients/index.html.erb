<h2>Patients</h2>
<ul>
  <% @patients.each do |patient| %>
    <li>
      Name: <%= link_to patient.name, patient_path(patient.id) %> Number of Appointments: <%= patient.appointments.count.to_s %>
    </li>
  <% end %>
</ul>
