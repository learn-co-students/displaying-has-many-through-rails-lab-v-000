<p id="notice"><%= notice %></p>

<h1>Listing Patients</h1>
<ul>
  <% @patients.each do |p| %>
    <li> Name: <%= p.name %> </li>
    <li> Number of Appointments: <%= p.appointments.length %> </li>
  <% end %>
</ul>
