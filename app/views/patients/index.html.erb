<h2>Patients</h2>

<ul>
  <% @patients.each do |patient| %>
    <li>Name: <%= link_to patient.name, patient_path(patient) %>
    Number of Apppointments:
    <%= patient.appointments.count %></li>
  <% end %>
</ul>
