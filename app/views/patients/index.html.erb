<h2>Patient Index</h2>

<ul>
  <% @patients.each do |patient| %>
    <li>Name: <%= link_to patient.name, patient_path(patient) %> <br>
    Number of Appointments: <%= patient.appointment_count %></li>
  <% end %>
</ul>
