<h1>All Patients</h1><br>
<ul>
  <% @patients.each do |patient| %>
    <li><h3>Name: <%= link_to patient.name, patient_path(patient)%> Number of Appointments: <%= patient.appointment_count %></h3></li>
  <% end %>
</ul>
