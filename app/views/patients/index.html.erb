<ul>	
	<%= @patients.each do |patient| %>
		<li>
			<h3>Name: <%= patient.name %> Number of Appointments: <%= patient.appointments.count %></h3>
			<%= link_to patient.name, patient_path(patient) %>
		</li>
	<% end %>
</ul>