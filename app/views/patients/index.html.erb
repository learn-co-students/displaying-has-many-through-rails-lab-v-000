<h1> Patients </h1>

<div>
  <ul>
    <% @patients.each do |p| %>
      Name: <li><%= link_to p.name, patient_path(p) %></li>
      Number of Appointments: <li><%= p.appointment_count %></li>
    <% end %>
  </ul>




</div>
