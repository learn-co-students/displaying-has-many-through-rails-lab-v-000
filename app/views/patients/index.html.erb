<h1>List of patients</h1>

<ul>
    <% @patients.each do |p| %>
        <li>
        Name: <%= link_to p.name, patient_path(p) %><br>
        Number of Appointments: <%= p.appointments.count%><br>
        </li>
    <%end%>
</ul>