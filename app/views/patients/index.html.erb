<h1>All Patients</h1>

<ul>
<% @patients.each do |patient| %>
  <li>
    Name: <%= link_to patient.name, patient_path(patient) %><br>
    <%= "Number of Appointments: #{patient.appointments.size}" %>
  </li>
<% end %>
</ul>
