<!-- patients/index -->

<h1>Patients</h1>

<ul>
  <% @patients.each do |patient| %>
    <li>Name: <%= patient.name %> Number of Appointments: <%= patient.app_count %></li>
  <% end %>
</ul>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Number of Appointments</th>
    </tr>
  </thead>

  <tbody>
    <% @patients.each do |patient| %>
      <tr>
        <td><%= patient.name %></td>
        <td><%= patient.app_count %></td>
      </tr>
    <%end%>
  </tbody>
</table> -->
