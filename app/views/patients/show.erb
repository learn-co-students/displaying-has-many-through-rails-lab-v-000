<h2><%= @patient.name %></h2>

<h3>Appointments:</h3>

<% @patient.appointments.each do |a| %>
	<%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to a.doctor.name, doctor_path(a.doctor) %> <br><br>
<% end %>