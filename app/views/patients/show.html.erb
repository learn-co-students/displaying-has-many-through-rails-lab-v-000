<h1>Patient: <%= @patient.name %></h1>
<h3>Age: <%= @patient.age %></h3>

<p>Appointments:</p>
<ul>
  <% @patient.appointments.each do |appointment| %>
      <li>Time: <%= appointment.format_date %> | Doctor: <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></li>
  <% end %>
</ul>
<!--shows all patients appointment times in readable format
and links to doctors show page by the name for each appointment-->
