<h1><%= @patient.name %></h1>
<ul>
<%@patient.appointments.each do |appt| %>
  <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
  <%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M %p")  %></li>
    <%end%>
</ul>
