<h2>Appointments for <%= @patient.name %></h2>
<% @patient.appointments.each do |appointment| %>
  <p><%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with Dr. <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></p>
<% end %>
