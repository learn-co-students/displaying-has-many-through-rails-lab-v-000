<h1><%= @patient.name %></h1>
 <h2>Appointments:</h2>
<% @patient.appointments.each do |appt| %>
    <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> <br>
    <%# "January 12, 2016 at 3:00" %>
<% end %>
