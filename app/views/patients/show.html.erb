<h1>Appointments for <%= @patient.name %></h1>
<ul>
<% @patient.appointments.each do |appt| %>
  <li><%= link_to appt.datetime, appointment_path(appt) %> with <%= link_to appt.doctor_name, doctor_path(appt.doctor) %></li>
<% end %>
</ul>
