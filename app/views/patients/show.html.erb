<h1>Patient: <%= @patient.name %></h1>
<% @patient.appointments.each do |appt| %>
  Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %><br>
  Appointment Date & Time: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")
 %>
  <br></br>
<% end %>
