<h3><%= @patient.name %></h3>
<hr>

<ol>
  <% @patient.appointments.sort_by{|a| a.appointment_datetime}.each do |a| %>
    <li>
      <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
       - <%= link_to a.doctor.name, doctor_path(a.doctor) %>
    </li>
  <% end %>
</ol>
