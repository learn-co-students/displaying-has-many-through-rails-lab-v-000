<h1><%= @patient.name %></h1>

<h3>Appointments</h3>

<% @patient.appointments.each do |appt| %>
  <strong><p>Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></p></strong>
  <p>Time: <%= appt.appointment_datetime.strftime(" %B %e, %Y at %k:%M") %></p>
<% end %>
