<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |app| %>
    <li>
      Doctor: <%= link_to app.doctor.name, doctor_path(app.doctor) %>
      Appointment: <%= app.app_date %>
    </li>
  <% end %>
</ul>
