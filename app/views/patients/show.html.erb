<h2><%= @patient.name %>'s Appointments</h2>
  <ul>
    <% @patient.appointments.each do |appt| %>
      <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %>: <%= appt.appointment_datetime.to_formatted_s(:long).insert(-6, "at ") %></li>
    <% end %>
  </ul>
