<h1><%= @patient.name %></h1>

<h2>Appointments</h2>
  <ul>
    <% @patient.appointments.each do |appt| %>
    <li> <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> at
      <%= appt.formatted_appt_time %></li>
      <% end %>
  </ul>
