<h1><%=@patient.name%></h1>

<h2>Appointments</h2>
<ul>
  <% @patient.appointments.each do |app| %>
      <li><%= app.datetime %> with <%= link_to app.doctor.name, doctor_path(app.doctor) %></li>
  <% end %>
</ul>
