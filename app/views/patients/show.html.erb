<h1><%= @patient.name%></h1>

Appointments:
<ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= appt.normalize_time %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor)%></li>
    
  <% end %>
</ul>