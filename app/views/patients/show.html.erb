<h1>Patient: <%= @patient.name %></h1>

<h1>Appointments</h1>
<ul>
<% @patient.appointments.each do |appt| %>
  <li>
  on <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> 
  with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
  </li>
  <% end %>
</ul>