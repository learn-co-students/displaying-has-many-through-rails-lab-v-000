<h1><%= @patient.name%></h1>

<ul>
  <% @patient.appointments.each do |app| %>
    <%= link_to app.doctor.name, doctor_path(app.doctor) %>, <%= app.appointment_datetime.strftime("%B %-d, %Y at %H:%M") %>
  <%end%>
</ul>
