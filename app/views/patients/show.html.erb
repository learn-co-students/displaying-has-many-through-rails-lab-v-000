<h1><%= @patient.name %></h1>
<h2>Appointments:</h2>
<% @patient.appointments.each do |appt| %>
	<%= appt.appointment_datetime.strftime("%B %-d, %Y at %H:%M") %><br>
	<%= link_to appt.doctor_name, doctor_path(appt.doctor) %><br>
<% end %>