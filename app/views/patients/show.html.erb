<h1>Patient <%=@patient.name%>'s appointments</h1>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li>
      <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with <%= link_to appointment.doctor.name, appointment.doctor %>
    </li>
    <%end%>
</ul>