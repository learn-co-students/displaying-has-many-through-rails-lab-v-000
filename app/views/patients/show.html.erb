<h1><%= @patient.name %></h1>
<h3>Appointments</h3>
<ul>
  <li><% @patient.appointments.each do |app| %>
        <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to  app.doctor.name, doctor_path(app.doctor) %></li>
      <% end %>
</ul>
