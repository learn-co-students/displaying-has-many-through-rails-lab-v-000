<%= @patient.appointments.each do |appt|%>
<%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
 <%= link_to @doctor.name, doctor_path(@doctor) if @doctor.id == appt.doctor_id %>
 <% end %>
