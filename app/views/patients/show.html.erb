<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> -
      <%= link_to(appointment.doctor) do %>
        <%= appointment.doctor.name %>
      <% end %>
    </li>
  <% end %>
</ul>
