<h2>Patient: <%= @patient.name %></h2>

<h3>Appointments</h3>
<% @patient.appointments.each do |appointment| %>
  <p>Doctor: <%= link_to appointment.doctor.name, appointment.doctor %> | 
  Date: <%= appointment.appointment_datetime.strftime("%B %-d, %Y at %H:%M") %></p>
<% end %>