<h1><%= @patient.name %></h1>

<p>Appointments:</p>
<ul>
  <% @patient.appointments.each do |appt| %>
    <li>
      <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> : <%= link_to appt.doctor.name, doctor_path(appt.doctor.id)%>
    </li>
  <% end %>
</ul>
