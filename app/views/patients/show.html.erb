<h2><%= @patient.name %></h2>

<%= @patient.appointments.each do |appt| %>
  <p>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
    <% doctor = Doctor.find(appt.doctor_id) %>
    <%= link_to doctor.name, doctor_path(appt.doctor_id) %>
  </p>
<% end %>
