<h2><%= @patient.name %></h2>

Appointments:
  <% @patient.appointments.each do |appointment| %>
    <p><%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> at
    <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
  <% end %>