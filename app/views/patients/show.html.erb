<h2><%= @patient.name %>'s doctor appointments</h2>

<% @patient.appointments.each do |app| %>
<p><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> <%= link_to app.doctor.name, app.doctor %></p>
<% end %>
