<% @patient.appointments.each do |appointment| %>
  <h3><%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> - <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></h3>
<% end %>
