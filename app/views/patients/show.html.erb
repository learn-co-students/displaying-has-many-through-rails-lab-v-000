<h1> <%= @patient.name  %> </h1>

<ul>
  <% @patient.appointment.each do |a| %>
    <li><%= a.appointment_datetime.to_formatted_s(:long_ordinal) %></li>
    <li><%= link_to a.doctor.name doctor_path(a.doctor)%></li>
</ul>
