<h1><%= @patient.name %>'s Appoinments</h1>
<ul>
  <% @patient.appointments.each do |a| %>
    <li><%= a.format_datetime %>, <%= link_to a.doctor.name, doctor_path(a.doctor) %></li>
  <% end %>
</ul>
