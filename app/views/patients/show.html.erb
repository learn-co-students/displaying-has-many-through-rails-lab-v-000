

<%= @patient.name %>

<% @patient.appointments.each do |appointment|%>
  <li>
   <%= appointment.appointment_datetime.strftime('%B %d, %Y') %> at <%= appointment.appointment_datetime.strftime('%H:%M') %><br>
  <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>
</li>
<% end %>
