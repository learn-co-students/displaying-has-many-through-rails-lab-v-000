<h1><%= @patient.name %></h1>
<h2>
  <ul>
  Appointments:
  <% @patient.appointments.each do |app| %>
    <li>
      <%= app.appointment_datetime.strftime('%B %d, %Y at %k:%M') %>
      with <%= link_to app.doctor.name, doctor_path(app.doctor) %>
    </li>
  <% end %>
  </ul>
</h2>
