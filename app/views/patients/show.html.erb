<h3> Name: <%= @patient.name %>, Age: <%= @patient.age %> </h3>

<ul>
  <% @patient.appointments.each do |appt| %>
  <li>Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor)%><br>
    <%=appt.appointment_datetime.strftime('%B %d, %Y')%> at
    <%=appt.appointment_datetime.strftime('%H:%M')%></li>
  <%end%>
</ul>
