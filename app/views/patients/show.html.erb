Patient Page

Name: <%= @patient.name %>

Appointment Times:
<ul>
  <%= @patient.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime('%B %d, %Y at %H:%M') %></li>
  <% end %>
</ul>
