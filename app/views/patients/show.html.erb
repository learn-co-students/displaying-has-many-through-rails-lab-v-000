<h1><%= @patient.name %></h1>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= link_to "#{appointment.doctor.name}: #{appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M")}", doctor_path(appointment.doctor)%></li>
  <% end %>
</ul>
