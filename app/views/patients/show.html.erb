<%=@patient.appointments.each do |appt|%>

<%=appt.appointment_datetime.strftime('%B %d, %Y')%> at <%=appt.appointment_datetime.strftime('%R')%> 
with Doctor: <%=link_to appt.doctor.name, doctor_path(appt.doctor) %><br>

<%end%>