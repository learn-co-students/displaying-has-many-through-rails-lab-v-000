<h1><%=@patient.name%></h1>

<ul>
  <%@patient.appointments.each do |appt|%>
    <li><%=link_to appt.appt_date, appointment_path(appt)%> - <%=link_to appt.doctor.name, doctor_path(appt.doctor)%></li>
  <%end%>
</ul>