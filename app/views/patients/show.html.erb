<p>
  Patient <strong><%= @patient.name %></strong>, age <%= @patient.age %>, 
  currently has <%= @patient.number_of_appointments %> appointments:
<p>

<!-- Fiddle around with that; use #pluralize on "appointments" (not required, just for fun). -->