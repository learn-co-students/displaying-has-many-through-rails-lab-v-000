<h1>Patient <%= @patient.name %></h1>
 
 <ul>
  <% @patient.appointments.each do |c| %>
    <li><%= link_to c.doctor_name, doctor_path(c.doctor) %></li>
    <p><%= c.appointment_readable %></p>
  <% end %>
</ul>