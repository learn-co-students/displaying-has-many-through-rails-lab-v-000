<h1><%= @patient.name %></h1>

<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= appointment.readable_time %> with <%= link_to appointment.doctor_name, doctor_path(appointment.doctor) %></li>
  <% end %>
</ul>
