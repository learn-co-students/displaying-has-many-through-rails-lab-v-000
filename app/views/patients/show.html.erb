<h1><%= @patient.name %></h1><br>
<h3>Appointments</h3>
<% @patient.appointments.each do |appt| %>
	<p><%= appt.appointment_datetime.strftime('%B %d, %Y at %H:%M') %> with 
		<%= link_to(appt.doctor.name, doctor_path(appt.doctor)) %>
<% end %>
