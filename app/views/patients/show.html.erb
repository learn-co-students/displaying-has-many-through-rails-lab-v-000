<h2>Doctor: <%= @patient.name %></h2>
<h3>
  Age: <%= @patient.age %>
</h3>
Appointments:
<table>
  <tr>
    <th>Date and Time</th>
    <th>Doctor</th>
  </tr>
  <tr>
  	<% @patient.appointments.each do |appointment| %>
    		<td><%= appointment.appointment_datetime %></td>
    		<td><%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></td>
    <% end %>
  </tr>
</table>
  
  