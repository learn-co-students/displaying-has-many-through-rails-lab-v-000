<h1><%= @patient.name %></h1>
Appointments:
<ul>
    <% @patient.appointments.each do |appt| %>
        <li><%= link_to appt.format_date, appt %>, with: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
    <%end%>
</ul>