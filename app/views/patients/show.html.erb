<h2><%= @patient.name %></h2>

<% @appointments.each do |appointment| %>
  Dr. <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %><br>
  <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br><br>
<% end %>