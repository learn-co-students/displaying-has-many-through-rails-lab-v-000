<h2><%= @patient.name %></h2>
<p>
  Name: <%= @patient.name %>
  Age: <%= @patient.age %>
</p>
Appointments :
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li><%= link_to appointment.doctor.name, doctor_path(appointment.doctor.id) %> 
    <%= appointment.appointment_datetime.strftime("%B") %> <%= appointment.appointment_datetime.day %>, <%= appointment.appointment_datetime.year %> at <%= appointment.appointment_datetime.strftime("%H:%M") %>.</li>
  <% end %>
</ul>