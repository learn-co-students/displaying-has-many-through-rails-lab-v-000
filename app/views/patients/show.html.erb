<h2><%= @patient.name %></h2>
<ul>
  <%= @patient.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %R") %></li>
    <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
  <% end %>
</ul>
