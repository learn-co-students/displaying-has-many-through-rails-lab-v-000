<h1>Patient#show</h1>

<h1><%= @patient.name %></h1>


<% @patient.appointments.each do |appt| %>
  <%= "#{appt.appointment_datetime.strftime('%B %d, %Y')} #{appt.appointment_datetime.strftime("at %R")}"%>
  <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
<% end %>