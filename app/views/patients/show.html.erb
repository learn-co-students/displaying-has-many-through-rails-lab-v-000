<ul>
    <%@patient.appointments.each do |appt| %>
    	<li>Doctor: <%= link_to(appt.doctor.name, doctor_path(appt.doctor)) %>
    	at: <%=appt.appointment_datetime.strftime('%B %d, %Y at %H:%M')%></li>
    <%end%>
</ul>