<h1>Appointment Information For: <%= @patient.name %></h1>
 <% @patient.appointments.each do |appt| %>
  <u1>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M ") %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
  </ul>
<% end %>
