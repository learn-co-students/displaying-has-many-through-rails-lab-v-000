<h2><%= @patient.name %></h2>

Appointments:
  <% @patient.appointments.each do |apt| %>

    <%= link_to apt.doctor.name, doctor_path(apt.doctor) %>
    <%= apt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
  <% end %>
