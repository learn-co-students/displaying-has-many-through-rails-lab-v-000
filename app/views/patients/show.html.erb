<h2>Name: <%= @patient.name%></h2>
<br>

<h3>  Age: <%= @patient.age %></h3>

Patient:
  <h3><% @patient.appointments.each do |appt| %>
    Name: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
    <br>
    Appointment: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></h3>
  <% end %>
