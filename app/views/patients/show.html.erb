<h1><%= @patient.name %></h1>

<h3><%= @patient.age %></h3>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li> <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> - Scheduled for <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M%p") %> </li>
  <% end %>
</ul>
