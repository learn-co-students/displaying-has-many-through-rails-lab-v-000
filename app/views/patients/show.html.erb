<h2>Appointments for <%= @patient.name %></h2>
<ul>
<% @patient.appointments.each do |appt| %>
  <li><%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor)%></li>
<% end %>
</ul>
