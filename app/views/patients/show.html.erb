<h2><%= @patient.name %></h2> has the following appointments:
<ul><% @patient.appointments.each do |appointment| %>
    <li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> with 
    Dr. <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>
    </li>
    <% end %>
</ul>