<h1><%= @patient.name %></h1>

<p>Appointments:</p>
<% @patient.appointments.each do |app| %>
  <p><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with <%= link_to app.doctor.name, doctor_path(app.doctor)%></p>
<% end %>
