<h2>Welcome <%=@patient.name%></h2>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li>Date: <%=appointment.appointment_datetime.strftime("%B %-d, %Y at %H:%M")%> - Doctor: <%=link_to appointment.doctor.name, appointment.doctor%></li>
  <% end %>
</ul>
