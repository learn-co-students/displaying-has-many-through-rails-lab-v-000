<%= @patient.name %>
<br><br>
<% @patient.appointments.each do |appointment| %>
  <%= link_to appointment.doctor.name, doctor_path(appointment.doctor.id) %> - <%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M') %><br>
<% end %>
