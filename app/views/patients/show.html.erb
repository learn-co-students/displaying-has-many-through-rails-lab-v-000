<h2><%= @patient.name %> Appointments:</h2>


<ul>
  <% @patient.appointments.each do |appointment| %>
  <li><%= appointment.convert %> <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></li>
  <% end %>
</ul>
