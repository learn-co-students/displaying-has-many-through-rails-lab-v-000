<h1><%= @patient.name %></h1>
<h3>Appointments</h3>
<% @patient.try(:appointments).try(:each) do |appointment| %>
  <p><%= appointment.appointment_datetime.strftime "%B %e, %Y at %H:%M" %><br>
    with <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></p>
<% end %> 
