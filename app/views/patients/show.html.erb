<% @appointments.each do |appt| %>
<ul>
  <li><%= link_to appt.doctor.name, doctor_path(appt.doctor) %><%= appt.appointment_datetime.strftime("%B %d, %Y") + " at " +  appt.appointment_datetime.strftime("%R") %></li>
</ul>
<%end%>
