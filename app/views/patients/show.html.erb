<h2><%= @patient.name %>'s Appointments</h2>

<% @patient.appointments.each do |appt| %>
  <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
  with Dr. <%= link_to appt.doctor.name, doctor_path(appt.doctor) %><br>
<% end %>
