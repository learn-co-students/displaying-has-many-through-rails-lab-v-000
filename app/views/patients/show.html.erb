<h1><%= @patient.name %></h1>

<ul>
  <% if @patient.appointments.count != 0 %>
    <% @patient.appointments.each do |appt| %>
      <li><%= appt.proper_format %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
    <% end %>
  <% end %>
</ul>
