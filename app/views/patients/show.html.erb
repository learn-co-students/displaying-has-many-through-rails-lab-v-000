<!-- Create a Doctor#show page that displays the patient's name, department, and appointments, with each appointment's date,
time, and patient name (linking to the patient's show page). -->

<h2><%= @patient.name %></h2>
<p>
  Age: <%= @patient.age %>
</p>
Appointments:
  <ul>
    <% @patient.appointments.each do |apt| %>
      <li><%= apt.appointment_datetime.strftime('%B %d, %Y') %>  <%= apt.appointment_datetime.strftime("at %H:%M") %>-
      <%= link_to apt.doctor.name, doctor_path(apt.doctor) %></il>
    <% end %>
  </ul>
