<h1>Patient: <%= @patient.name %></h1>

<ul>
  <%= @patient.appointments.each do |app| %>
    <li><%=app.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> - <%= link_to(app.doctor_name, doctor_path(app.doctor)) %></li>
  <% end %>
</ul>