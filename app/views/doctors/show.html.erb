<<%@doctor.appointments.each do |appointment|%>
<%=appointment.appointment_datetime.strftime("%B %e")%>, <%=appointment.appointment_datetime.strftime("%Y")%> at <%=appointment.appointment_datetime.strftime("%H:%M") %>
<%=link_to appointment.patient.name, patient_path(appointment.patient)%>
<%end%>
