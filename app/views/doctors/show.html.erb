<h1>Doctors#show</h1>
<% @doctor.appointments.each do |appt| %>
  <p><%= appt.appointment_datetime.strftime("%B %-d, %Y at %H:%M") %>: <%= link_to appt.patient.name, patient_path(appt.patient) %></p>
<% end %>
