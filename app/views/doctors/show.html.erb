<h2>Dr. <%= @doctor.name %></h2>
<h3><%= @doctor.department %></h3>

<h4>Appointments:</h4>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.readable_time %> - <%= link_to appt.patient_name, patient_path(appt.patient) %></li>
  <% end %>
</ul>