<h1><%= @doctor.name %></h1>

<ul>
	<% @doctor.appointments.each do |a| %>
		<li>

			Appointment: <p><%= a.readable_appointment_datetime %><p><br>
			Patient: <p><%= link_to a.patient.name, patient_path(a.patient) %></p>
		</li>
	<% end %>
</ul>