<% @appointments.each do |appt| %>
<ul>
  <li><%= link_to appt.patient.name, patient_path(appt.patient) %><%= appt.appointment_datetime.strftime("%B %d, %Y") + " at " +  appt.appointment_datetime.strftime("%R") %></li>
</ul>
<%end%>
