<h2>Welcome <%=@doctor.name%></h2>

<ul>
<% @doctor.appointments.each do |appointment| %>
  <li>Date: <%=appointment.appointment_datetime.strftime("%B %-d, %Y at %H:%M")%> - Patient: <%=link_to appointment.patient.name, appointment.patient%></li>
<% end %>
</ul>
