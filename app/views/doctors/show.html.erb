<ul>
<% @doctor.appointments.each do |appt| %>
  <li><%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %> with <%= link_to appt.patient.name, patient_path(appt.patient)%></li>

<% end %>
</ul>