<h1><%= @doctor.name %></h1>
 <h2>Appointments:</h2>
<% @doctor.appointments.each do |appt| %>
    <%= link_to appt.patient.name, patient_path(appt.patient) %> <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> <br>
    <%# "January 12, 2016 at 3:00" %>
<% end %> 
