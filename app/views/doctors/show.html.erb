<h2><%= @doctor.name %> - <%= @doctor.department %></h2>
<h3>Appointments:</h3>
<ul>
 <% @doctor.appointments.each do |app| %>
   <li><%= link_to app.patient_name, patient_path(app.patient) %> on <%= app.formatted_date %></li>
 <% end %>
</ul>
