<!-- page that displays the doctor's name, department, and appointments, with each appointment's date, time, and patient name (linking to the patient's show page). -->

Doctor: <%= @doctor.name %><br>
Department: <%= @doctor.department %><br>

<h3>Appointments </h3>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li> <%= appt.appointment_datetime.strftime("%B %-d, %Y") %> at <%= appt.appointment_datetime.strftime("%H:%M") %> with <%= link_to appt.patient.name, patient_path(appt.patient.id) %>
  <% end %>
</ul>
