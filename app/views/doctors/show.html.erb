<%= @doctor.inspect %>
<% @doctor.appointments.each do |appointment| %>
  <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
  <%= link_to appointment.patient.name, appointment.patient %><br>
  <%= appointment.patient.age.to_s %><br>
<% end %>
