<h1>Dr. <%= @doctor.name %></h1>

<table>
	<tr>
		<th>Patient</th>
		<th>Date</th>
	</tr>
	<% @doctor.appointments.each do |appt| %>
		<tr>
			<td><%= link_to appt.patient.name, patient_path(appt.patient) %></td>
			<td><%= appt.readable_format %></td>
		</tr>
	<% end %>
</table>
