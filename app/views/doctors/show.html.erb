Doctor Name: <%= @doc.name %>
Department: <%= @doc.department %><br>
<ul>
  <li>
    <% @doc.appointments.each do |app| %>
    <%= app.readable_time%>
    <%= link_to app.patient.name, patient_path(app.patient) %><br>
    <% end %>
  </li>
</ul>