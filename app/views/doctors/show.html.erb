<%= @doctor.name %>
<%= @doctor.department %>
<% @doctor.appointments.each do |p| %>
<%= link_to p.patient.name, patient_path(p.patient) %>
<%= p.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> 
<% end %>
