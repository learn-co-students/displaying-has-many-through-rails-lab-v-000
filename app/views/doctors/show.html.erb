<h1>Dr. <%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |app| %>
    <li>on <%= app.datetime %> with <%= link_to app.patient.name, patient_path(app.patient) %></li>
  <% end %>
</ul>
