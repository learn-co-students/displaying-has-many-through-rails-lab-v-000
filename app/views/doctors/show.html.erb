<h1><%= @doctor.name %>'s Appointments:</h1>
<ul>
  <% @doctor.appointments.each do |apt|  %>
    <li><%= apt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> with <%= link_to apt.patient.name, patient_path(apt.patient) %></li>
  <% end %>
</ul>
