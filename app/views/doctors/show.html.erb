These are all of <%= @doctor.name %>'s Appointment
<% @doctor.appointments.each do |app| %>
<p><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> with patient <%= link_to app.patient.name, app.patient %></p>

<% end %>
