<h2><%= @doctor.name %> Appointments:</h2>

<ul>
  <% @doctor.appointments.each do |appointment| %>
  <li><%= appointment.convert %> <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
