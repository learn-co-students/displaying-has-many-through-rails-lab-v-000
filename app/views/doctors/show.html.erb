<h1>Doctor <%=@doctor.name%>'s Appointments:</h1>
<ul>
  <%@doctor.appointments.each do |apt|%>
  <li><%=apt.time%> with <%= link_to apt.patient_name, patient_path(apt.patient)%></li>
  <%end%>
</ul>
