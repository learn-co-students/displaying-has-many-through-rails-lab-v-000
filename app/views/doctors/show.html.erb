<h1><%=@doctor.name%></h1>

<ul>
  <%@doctor.appointments.each do |appt|%>
    <li><%=link_to appt.appt_date, appointment_path(appt)%> - <%=link_to appt.patient.name, patient_path(appt.patient)%></li>
  <%end%>
</ul>