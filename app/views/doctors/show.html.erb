<h1><%= @doctor.name %></h1>

<ul>
    <% @doctor.appointments.each do |appt| %>
        <li><%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> - <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
    <% end %>
</ul>