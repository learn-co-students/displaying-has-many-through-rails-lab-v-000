<h1>Appointments for <%= @doctor.name %></h1>
<ul>
<% @doctor.appointments.each do |appt| %>
  <li><%= link_to appt.datetime, appointment_path(appt) %> with <%= link_to appt.patient_name, patient_path(appt.patient) %></li>
<% end %>
</ul>
