<h1><%= @doctor.name %></h1>
<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.patient.name, patient_path(appointment.patient) %>, <%= appointment.human_time %></li>
  <% end %>
</ul>
