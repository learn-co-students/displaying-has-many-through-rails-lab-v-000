<h1>All Appointments</h1>

<!-- # doctors #show page shows all of a doctor's appointment times in a human readable format -->

<%= @doctor.appointments.each do |appt|  %>
  <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M%p") %><br>
  <%= link_to appt.patient.name, patient_path(appt.patient) %><br>
<% end %>