<h1><%= @doctor.name %></h1>

<p>Appointments:</p>
<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= app.date %> with <%= link_to app.patient.name, patient_path(app.patient) %></li>
  <% end %>
</ul>