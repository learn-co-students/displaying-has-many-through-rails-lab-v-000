<h2><%= @doctor.name %></h2>
<h4><%= @doctor.department %></h4>
<%= @doctor.appointments.all.each do |appt| %>
  <%= link_to appt.patient.name, patient_path(appt.patient) %>
  has an appointment at
  <%= appt.appointment_datetime %>
  <br>
<% end %>
