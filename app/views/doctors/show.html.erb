<h2>Docotr <%= @doctor.name %></h2>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>Patient Name: <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
    <li>Appointment Time: <%= appt.normalize_time %></li>
  <% end %>
</ul>
