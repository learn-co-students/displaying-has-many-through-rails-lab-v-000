<% @doctor.appointments.each do |appointment| %>
  <%= link_to "#{appointment.appointment_datetime.strftime("%B %d, %Y")} at #{appointment.appointment_datetime.strftime("%H:%M")}", appointment_path(appointment) %>
  <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
<% end %>
