<h2><%= @doctor.name %>'s Appointments</h2>
  <ul>
    <%= @doctor.appointments.each do |appt| %>
      <li><%= link_to appt.patient.name, patient_path %>: <%= appt.appointment_datetime %></li>
    <% end %>
  </ul>
