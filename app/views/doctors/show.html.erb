<h1><%= @doctor.name %></h1>

<h2>Appointments:</h2>
<% @doctor.appointments.each do |appt| %>
  <%= link_to appt.patient.name, patient_path(appt.patient) %> (<%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %>)<br />
<% end %>
