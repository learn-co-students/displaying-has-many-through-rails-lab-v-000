<% @doctor.appointments.each do |appt| %>
  Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %><br>
  Time: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br><br>
<% end %>