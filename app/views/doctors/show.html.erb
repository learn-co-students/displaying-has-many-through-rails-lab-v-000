<h1><%=@doctor.name%></h1>

<ul>
    <% @doctor.appointments.each do |appt|%>
    <li> <%= appt.date_time %> with <%= link_to appt.patient_name, patient_path(appt.patient)%> </li>
    <% end %>
    
</ul>