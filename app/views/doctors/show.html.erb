<h1>Doctor <%= @doctor.name %></h1>

<h3>Department: <%= @doctor.department %></h3>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>
      <div>
        <%= appt.formatted_time %> <br>
        <%= link_to appt.patient.name, patient_path(appt.patient) %>
      </div>
    </li>
  <% end %>
</ul>