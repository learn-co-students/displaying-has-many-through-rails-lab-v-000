<h2> <%= @doctor.name %>, <%= @doctor.department %> </h2>

<h3> Appointments: </h3>
<ul>
  <% @doctor.appointments.each do |appointment| %>
  <li> <%= link_to appointment.patient.name, patient_path(appointment.patient) %>, <%= appointment.date %> at <%= appointment.time %> </li>
  <%end %>
</ul>
