<h1><%= @doctor.name %></h1>

<h3>Appointments:</h3>
<ul>
  <%= @doctor.appointments.each do |appointment| %>
    <li>
      <%= link_to appointment.patient_name, patient_path(appointment.patient) %> on <%= appointment.datetime %>
    </li>
  <% end %>
</ul>
