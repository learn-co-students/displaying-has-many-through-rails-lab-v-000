<h2>Doctor: <%= @doctor.name %></h2>

<h3>Appointments: </h3>
<ul>
<% @doctor.appointments.map do |app| %>
  <li><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>  <%=link_to  app.patient.name, patient_path(app.patient)%></li>
<% end %>
</ul>
  
