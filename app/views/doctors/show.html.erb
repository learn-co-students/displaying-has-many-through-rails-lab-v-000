<h1><%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.proper_format %> with <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
  <% end %>
</ul>
