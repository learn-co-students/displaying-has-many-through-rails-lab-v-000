<p id="notice"><%= notice %></p>

<p>
  <strong>Appointments for:</strong>
  <%= @doctor.name %>
</p>

<% @appointments.each do |app| %>
	<li>
		Time: <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
		Patient: <%= link_to "#{app.patient.name}", patient_path(app) %>
	</li>
<%end%>

