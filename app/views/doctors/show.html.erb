<h1> <%= @doctor.name %> </h1>
<h3> <%= @doctor.department %> </h3>

<ul>  
  <% @doctor.appointments.each do |appointment| %>
    <li>
      <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
      <%= appointment.human_readable_time %>
    </li>
  <% end %>
</ul>