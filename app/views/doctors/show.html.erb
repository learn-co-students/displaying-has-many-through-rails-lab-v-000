<h1><%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |a| %>
    <li><%= link_to a.friendly_datetime, appointment_path(a) %></li>
    <li><%= link_to a.patient_name, patient_path(a.patient) %></li>
  <% end %>
</ul>
