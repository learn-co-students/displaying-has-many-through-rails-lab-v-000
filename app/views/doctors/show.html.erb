<h1>Doctor's Name: <%= @doctor.name %></h1>

<h3>Appointments</h3>

<p>

<ul>
  <% @doctor.appointments.each do |app| %>
    <% t = app.appointment_datetime %>
    <li>
      Meeting with <%= link_to app.patient.name, app.patient %> on <%= t.strftime('%B') %>
      <%= t.strftime('%d') %>, <%= t.strftime('%G') %> at <%= t.strftime('%R') %>
    </li>
  <% end %>
</ul>

</p>
