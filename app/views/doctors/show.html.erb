<h1><%= @doctor.name %></h1>
<%= @doctor.department %>
<br>
<% @doctor.appointments.each do |appointment| %>
  <span><%= link_to appointment.patient.name, patient_path(appointment.patient) %> - <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></span>
<% end %>
