<h1><%= @doctor.name %> </h1>

<ul>
  <% @doctor.appointments.each do |appointment| %>
  <li><%= link_to appointment.patient.name, appointment.patient %>
    has appointment on:
    <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
    <%end %>
</ul>
