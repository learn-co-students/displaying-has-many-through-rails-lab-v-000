<h1>Doctor <%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |apt| %>
    <li>
      <%= link_to apt.patient.name, patient_path(apt.patient) %> : <%= apt.readable_time %>
    </li>
  <% end %>
</ul>