<h1><%= @doctor.name %>'s appointments:</h1>
<%= div_for(@doctor.appointments, class: "doctor-show-page") do |appt| %>
  <p>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><%= link_to appt.patient.name, patient_path(appt.patient) %>
  </p>
<% end %>
