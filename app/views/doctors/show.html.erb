<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2>

<h3>Appointments:</h3>
<ul>
    <% @doctor.appointments.each do |appt| %>
        <li><%= link_to appt.format_date, appt %> <%= link_to appt.patient_name, patient_path(appt.patient) %>, </li>
    <% end %>
</ul>