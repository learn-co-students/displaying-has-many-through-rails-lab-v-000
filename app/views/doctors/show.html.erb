<h1> Your Appointments </h1>

<% @appointments.each do |appt| %>
<h2><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>: <%= link_to appt.patient.name, patient_path(appt.patient) %></h2>
<% end %>
