<h1><%= @doctor.name %></h1>
<% @doctor.appointments.each do |doctor| %>
    <li>
        <%= link_to doctor.patient.name, patient_path(doctor.patient) %> <%= doctor.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
    </li>
<% end %>