<h1>Dr. <%= @doctor.name %></h1>

<h2><%= @doctor.department %></h2>

<h3>Appointments</h3>

<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.readable_datetime %>  <%= link_to appointment.patient_name, patient_path(appointment.patient) %></li><br>
  <% end %>
</ul>