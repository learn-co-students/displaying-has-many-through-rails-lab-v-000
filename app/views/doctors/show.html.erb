<h2><%= @doctor.name %></h2>
<h4>Department: <%= @doctor.department %></h4>

<h4>Appointments:</h4>
<ul>
<% @doctor.appointments.each do |appt| %>
  <li> <%= appt.date_format %>, Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
<% end %>
</ul>
