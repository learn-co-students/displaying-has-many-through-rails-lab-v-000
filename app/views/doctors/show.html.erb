<h3><%= @doctor.name %>'s List of Shit to Do</h3>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %R") %> with <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
  <% end %>
</ul>
