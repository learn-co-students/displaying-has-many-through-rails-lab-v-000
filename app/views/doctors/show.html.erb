<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> for <%= link_to app.patient.name, patient_path(app.patient) %></li>
  <% end %>
</ul>
