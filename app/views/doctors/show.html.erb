<h1><%=@doctor.name%></h1>
<p><%=@doctor.department%></p>

<h2>Appointments</h2>

<ul>
  <% @doctor.appointments.each do |appt|%>
    <li><%= appt.human_date_time%>
      <%= link_to appt.patient.name, patient_path(appt.patient)%></li><br>
  <%end%>
</ul>
