<h1><%= @doctor.name %>
<h2><%= @doctor.department %>
Appointments: <% @doctor.appointments.each do |appointment| %>
  <h3><%= appointment.appointment_date %></h3>
  <h3><%= link_to appointment.patient_name, patient_path(appointment.patient) %></h3>
<% end %>
