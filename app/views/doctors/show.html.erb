<%= @doctor.name %>
<%= @doctor.department %>
<%= @doctor.appointments.each do |appointment| %>
<%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
<%= link_to @patient.name, patient_path(@patient) if appointment.patient_id== @patient.id %>
<% end %>
