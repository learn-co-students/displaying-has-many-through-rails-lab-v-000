<h3>Doctor : <%= @doctor.name %></h3>

<h3>All Appointments</h3>
<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= link_to app.patient.name, patient_path(app.patient)%> at <%= app.appointment_time %></li>
  <% end %>
</ul>
