<h1><%= @doctor.name %></h1>

Appointments:
<ul>
<% @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.patient_name, patient_path(appointment.patient_id) %>: <%= appointment.time %></li>
<% end %>
</ul>