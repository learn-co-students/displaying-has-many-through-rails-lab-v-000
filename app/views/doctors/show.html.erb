<h3><%= @doctor.name %></h3>
<h3><%= @doctor.department %></h3>
<% @doctor.appointments.each do |appt| %>
<ul><%= appt.appointment_datetime.strftime("%B %d, %Y at %T") %></ul>
<ul><%= link_to appt.patient.name, patient_path(appt.patient) %></ul>

<% end %>