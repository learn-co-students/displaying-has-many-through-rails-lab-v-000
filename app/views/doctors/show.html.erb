<h1><%= link_to @doctor.name, doctor_path(@doctor) %></h1>
<h3><%= @doctor.department %><h3>
<% @doctor.appointments.each do |appt| %>
  <p><%= link_to appt.patient.name, patient_path(appt.patient) %></p>
  <p><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M.") %></p><br>
<% end %>
