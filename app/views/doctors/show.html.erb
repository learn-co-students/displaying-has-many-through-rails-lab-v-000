<h2><%= @doctor.name %></h2>

Appointments:
  <% @doctor.appointments.each do |appointment| %>
  <p><%= link_to appointment.patient.name, patient_path(appointment.patient) %> at
    <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
  <% end %>