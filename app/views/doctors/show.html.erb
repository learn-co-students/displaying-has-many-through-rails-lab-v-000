<h2><%= @doctor.name %></h2>	
Department: <%= @doctor.department %>	
 <h3>Appointments</h3>	
<ul>	
  <% @doctor.appointments.each do |a| %>	
    <li><%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to a.patient.name, patient_path(a.patient) %></li>	
  <% end %>	
</ul>