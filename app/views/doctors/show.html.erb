<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.readable_datetime %> - Patient: <%= link_to appointment.patient_name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>