<h1>Doctor <%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |a| %>
    <div><%= link_to a.patient.name, patient_path(a.patient)%> :
      <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></div>
    <%end%>
</ul>
