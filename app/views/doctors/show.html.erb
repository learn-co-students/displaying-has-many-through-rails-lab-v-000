<h2><%= @doctor.name %></h2>

<ul>

  <% @doctor.appointments.each do |appt| %>
    <li>Appointment with <%= link_to appt.patient.name, patient_path(appt.patient) %>, <%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %></li>
  <% end %>
</ul>