<h2><%= @doctor.name %></h2>

<h3>Department</h3>
<p><%= @doctor.department %></p>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= appt.to_string %> - <%= link_to appt.patient_name, patient_path(appt.patient) %></li>
  <% end %>
</ul>
