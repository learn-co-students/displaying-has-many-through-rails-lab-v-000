<h1> <%= @doctor.name %> </h1>
<p> <%= @doctor.department %> </p>
Appointments:
<p> <% @doctor.appointments.each do |a| %>
    	<li><%= a.appointment_datetime.strftime("%B %e, %Y at %H:%M ")%></li>
    	<li><%= link_to a.patient.name, patient_path(a.patient)%></li>
    <% end %> 
</p>
