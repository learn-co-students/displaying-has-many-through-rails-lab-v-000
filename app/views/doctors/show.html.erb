<h1><%= @doctor.name %>'s Appointments</h1>

<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= link_to app.patient_name, patient_path(app.patient) %>: <%= app.readable %></li>
  <% end %>
</ul>
