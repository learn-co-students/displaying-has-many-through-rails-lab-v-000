h2><%= @doctor.name%></h2>
<br>

<h3>  Department: <%= @doctor.department %></h3>

Patient:
  <h3><% @doctor.appointments.each do |appt| %>
    Name: <%= link_to appt.patient.name, patient_path(appt.patient) %>
    <br>
    Appointment: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></h3>
  <% end %>
