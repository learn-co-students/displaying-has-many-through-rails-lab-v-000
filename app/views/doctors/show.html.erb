<ul>
    <%@doctor.appointments.each do |appt| %>
    	<li>Doctor: <%= link_to(appt.patient.name, patient_path(appt.patient)) %>
    	at: <%=appt.appointment_datetime.strftime('%B %d, %Y at %H:%M')%></li>
    <%end%>
</ul>