<h1><%= @doctor.name %></h1>

<h3><%= @doctor.department %></h3>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li> <%= link_to appt.patient.name, patient_path(appt.patient) %> - Scheduled for <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M%p") %> </li>
  <% end %>
</ul>
