<h1><%=@doctor.name%></h1>
<h2><%=@doctor.department%></h2>

<ul>
  <% @doctor.appointments.each do |apt|%>
    <li>
      <%=apt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> <%=link_to apt.patient.name, patient_path(apt.patient) %>
    </li>
  <%end%>
</ul>
