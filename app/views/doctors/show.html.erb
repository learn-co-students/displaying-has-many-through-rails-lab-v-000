<h1>Doctor <%= @doctor.name %></h1>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |apt| %>
  <li>
    <%= apt.appointment_datetime.strftime("%B %d, %Y at %H:%M %P") %>
      with <%= link_to apt.patient.name, patient_path(apt.patient) %>
  </li>
  <% end %>
</ul>
