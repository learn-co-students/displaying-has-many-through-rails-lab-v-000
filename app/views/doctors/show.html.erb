<%= @doctor.name %>
<%= @doctor.department %>

Appointments: <% @doctor.appointments.each do |appt| %>
<%= appt.datetime %><br>
<%= link_to appt.patient.name, patient_path(appt.patient) %>
<% end %>
  