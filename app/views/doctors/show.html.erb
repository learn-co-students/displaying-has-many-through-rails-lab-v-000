<h2><%= @doctor.name %></h2>

<% @doctor.appointments.each do |appointment| %>
 <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M %P") %> with <%= link_to appointment.patient.name, patient_path(appointment.patient)%>
 <%end%>
