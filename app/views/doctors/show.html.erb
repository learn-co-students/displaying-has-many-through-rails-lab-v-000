<h1><%= @doctor.name %>, MD</h1>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li>
      <%= appointment.formatted_time %> - <%= link_to appointment.patient_name, patient_path(appointment.patient) %>
    </li>
  <% end %>
</ul>
