<h2><%= @doctor.name %></h2>
<h3><%= @doctor.department %></h3>

<% @doctor.appointments.each do |appt| %>
  Appointment: <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %> for
  patient: <%= link_to appt.patient.name, patient_path(appt.patient) %><br>
<% end %>
