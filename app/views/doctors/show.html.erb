<h1><%=@doctor.name%> - <%=@doctor.department%></h1>
<ul>
  <%= @doctor.appointments.each do |appointment| %>
  <li><%=appointment.readable_appointment_datetime%> - <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
