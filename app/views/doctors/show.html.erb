<%= @doctor.name %>
<%= @doctor.department %>

<%@doctor.appointments.each do |apmt| %>
  <%= apmt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
  <%= link_to @patient.name, patient_path(@patient) if apmt.patient_id == @patient.id %>
<% end %>