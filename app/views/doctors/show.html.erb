<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to "#{appointment.patient.name}: #{appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M")}", patient_path(appointment.patient)%></li>
  <% end %>
</ul>
