<h1>Doctor <%=@doctor.name%></h1>

<h2>Appointments:</h2>
<ul>
  <% @doctor.appointments.each do |apt| %>
  <li>
    <%= link_to apt.patient.name, patient_path(apt.patient) %> - <%= apt.appointment_datetime.strftime('%B %d, %Y at %H:%M.')%>
  </li>
  <%end%>
</ul>
