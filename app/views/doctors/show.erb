<!-- displays the doctor's name, department, and appointments, with each appointment's date, time, and patient name (linking to the patient's show page). -->
<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2>

<h3>Appointments</h3>
<% @doctor.appointments.each do |appointment| %>
  <%= appointment.appointment_datetime.strftime('%B %d, %Y at %k:%M') %> --
  <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
  <br>
<% end %>
