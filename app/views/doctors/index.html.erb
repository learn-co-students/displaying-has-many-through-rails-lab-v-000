<h1>Doctor Index</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
       - 
      Patient Count: <%= doctor.patients.size %>
    </li>
  <% end %>
</ul>
