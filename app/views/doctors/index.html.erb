
#Create a Doctor's index page which displays each doctor's 
#name, a link to their show page, and a count of their patients.

 <% @doctors.each do |doctor| %>
    <%= link_to doctor.name, doctor_path(doctor) %> 
    Patients: <%= doctor.patient_count %>
  <% end %>