<h1>List of Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to doctor.name, doctor_path(doctor) %></li>
    <li><%= pluralize(doctor.patients.count, "Patient") %></li>
  <% end %>
</ul>
