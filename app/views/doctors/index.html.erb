<h1>Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
      <br />
      <%= doctor.patients.count %> patient(s)
        <br /><br />
    </li>
    <% end %>
</ul>
