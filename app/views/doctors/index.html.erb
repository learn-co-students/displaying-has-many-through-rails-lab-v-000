<h1>Doctors</h1>

<% @doctors.each do |doctor| %>
  <div>
    <h5><%= link_to doctor.name, doctor_path(doctor) %></h5>
    <p>Department: <%= doctor.department %></p>
    <p><%= doctor.patients_count %> patients served</p>
  </div>
  <hr>
<% end %>