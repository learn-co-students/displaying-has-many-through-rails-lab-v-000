<h1>Doctors</h1>

<%@doctors.each do |doctor|%>
  <h3><%=link_to("#{doctor.name} (#{doctor.department})", doctor_path(doctor))%></h3>
  <p><%=pluralize(doctor.patients.count, 'patient')%></p>
<%end%>
