<h1>All Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to doctor.name, doctor_path(doctor) %> has a total of <%= pluralize(doctor.patients.count, "patient") %></li>
  <% end %>
</ul>
