<h1> Doctors List <h2>
  <ul><% @doctors.each do |doctor| %>
    <li> <%= link_to doctor.name doctor_path(doctor) %></li>
    <label> Patient count: </label><%= doctor.patients.count %>
    <% end %>
  </ul>
