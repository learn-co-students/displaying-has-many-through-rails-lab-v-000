<h2>Hospital Doctors</h3>

  <ul>
    <% @doctors.each do |doctor| %>
      <li><%= link_to doctor.name, doctor_path(doctor) %> total patients: <%= doctor.patients.count %></li>
    <% end %>
  </ul>
