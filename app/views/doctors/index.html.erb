<h1>Doctors List</h1>

<ul>

<% @doctors.each do |doctor| %>

  <li>
    <%= link_to doctor.name, doctor_path(doctor) %> - <%= pluralize(doctor.appointments.count, 'appointment') %>
  </li>

<% end %>

</ul>
