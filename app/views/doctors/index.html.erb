<h1>Seattle Grace Hospital Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
      <%= pluralize(doctor.patients_count, 'patient') %>
    </li>
  <% end %>
</ul>
