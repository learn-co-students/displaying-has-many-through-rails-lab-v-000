<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
      <p>This doctor has <%= pluralize(doctor.patients.count, "patient") %>.</p>
    </li>
  <% end %>
</ul>
