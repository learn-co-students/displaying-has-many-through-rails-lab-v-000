<h1> Doctors </h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li>
      Name: <%= link_to doctor.name, doctor_path(doctor) %>
      &nbsp; &nbsp;
      Patients: <%= doctor.patients.count %>
    </li>
  <% end %>
</ul>