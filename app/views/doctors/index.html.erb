<div>
  <ul>
    <% @doctors.each do |doctor| %>
      <li>Name: <%= link_to doctor.name, doctor_path(doctor) %></li>
      <li>Patient Count: <%= doctor.patient_count %></li>
    <% end %>
  </ul>
</div>
